version: '3.8'

services:
  earnverse-bot:
    build: .
    container_name: earnverse_bot_app
    ports:
      - "8080:80"
    volumes:
      # Mount the current directory into the container's web root
      # This allows for live code changes during development without rebuilding the image
      - .:/var/www/html
    restart: unless-stopped

